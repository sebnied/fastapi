[tool.poetry]
name = "fastapi"
version = "0.1.0"
description = "Fastapi project"
authors = ["sebastianniedziela <seb.niedziela@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
